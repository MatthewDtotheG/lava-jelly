import styled from "styled-components";

const ContentContainer = styled.div`
  margin: 0px 15%;
  color: white;
`;

const About = () => {
  return (
    <ContentContainer>
      <h1>ABOUT PAGE</h1>
    </ContentContainer>
  );
};

export default About;
