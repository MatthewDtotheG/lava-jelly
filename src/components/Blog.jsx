import styled from "styled-components";

const ContentContainer = styled.div`
  margin: 0px 15%;
  color: white;
`;

const Blog = () => {
  return (
    <ContentContainer>
      <h1>BLOG PAGE</h1>
    </ContentContainer>
  );
};

export default Blog;
