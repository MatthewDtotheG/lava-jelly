import styled from "styled-components";

const ContentContainer = styled.div`
  margin: 0px 15%;
  color: white;
`;

const Apps = () => {
  return (
    <ContentContainer>
      <h1>APPS PAGE</h1>
    </ContentContainer>
  );
};

export default Apps;
